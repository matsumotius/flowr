script(type = 'text/javascript', src = '/socket.io/socket.io.js')
script
  $(function(){
    FLOWR.attribute.user = {
      exists    :  #{user.exists},
      _id       : "#{user._id}",
      name      : "#{user.name}",
      user_id   : "#{user.user_id}"
    };
    FLOWR.attribute.channel = {
      key   : "#{channel._id}",
      id    : "#{channel.channel_id}",
      name  : "#{channel.name}",
      queue : []
    };
  });
script(type='text/javascript', src='/js/' + type + '/control.js')
